#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {

	int x;
	scanf("Ouviram%d", &x);
	printf("%d\n", x);
	printf("[%c]\n", getchar());
	printf("[%c]\n", getchar());
	printf("[%c]\n", getchar());

	return 0;
}
